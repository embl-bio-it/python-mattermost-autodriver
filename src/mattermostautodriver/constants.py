#: Maximum number of items per page accepted by the Mattermost API.
MAX_PER_PAGE = 200

#: Default number of items per page used by the Mattermost API when ``per_page`` is omitted.
DEFAULT_PER_PAGE = 60
